services:
  cloudvault:
    build:
      context: .
      dockerfile: server/Dockerfile
    restart: unless-stopped
    ports:
      - "8787:8787"
    environment:
      # Set to "false" after you have created your account(s).
      ALLOW_REGISTRATION: "true"
    volumes:
      - cloudvault-data:/data

volumes:
  cloudvault-data:
