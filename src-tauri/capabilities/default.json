{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal permissions: the app needs no native APIs beyond the webview.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
