{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal permissions: only the webview core plus the app's own platform_info / steam_keyboard commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
